let i = 0;
while(true)
{
    if(i % 2 == 0)
    {
        console.log(i);
        
    }
    i++;
}

// && AND
// Condição A | Condição B | A && B
// true       | true       | true
// true       | false      | false
// false      | true       | false
// false      | false      | false

// // || OR
// Condição A | Condição B | A && B
// true       | true       | true
// true       | false      | true
// false      | true       | true
// false      | false      | false